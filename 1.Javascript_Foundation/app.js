console.log('hi');

let x = 4
let y = 7
let z = x + y
console.log( z)

const name = "John";
const age = 20;
const city = "Nakuru";
const radius = 10;
const pi = Math.PI;
const area = pi * (radius ** 2);

// Create a template string that includes these variables
const message = `Hello, my name is ${name}. I am ${age} years old and I live in ${city}.
Did you know that the area of a circle with a radius of ${radius} is ${area.toFixed(2)}?`;

// Output the result
console.log(message);
